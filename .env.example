# Local Docker database configuration. Prefer: cd backend; npm run db:init
# That command creates root .env with random values and never overwrites it.
POSTGRES_PORT=5433
POSTGRES_PASSWORD=replace-with-random-secret
SEED_PASSWORD=replace-with-random-secret
SEED_API_KEY=replace-with-random-secret
